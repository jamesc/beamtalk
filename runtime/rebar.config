{erl_opts, [debug_info]}.

{deps, [
    {jsx, "~> 3.0"}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]}
    ]}
]}.

{eunit_opts, [verbose]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.
