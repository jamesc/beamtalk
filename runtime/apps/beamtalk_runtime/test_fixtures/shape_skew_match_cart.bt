// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// BT-3602 (ADR 0126 §5.3, Phase 4) fixture: the identical module registered
// on *both* nodes of `beamtalk_dist_shape_skew_tests`' two-node pair — a
// shared class at matching `shapeVersion`s (1, implicit), which must never
// announce `NodeShapeSkew`.

Actor subclass: ShapeSkewMatchCart
  state: qty :: Integer = 0
