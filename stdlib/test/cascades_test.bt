// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// E2E tests for cascade message sends

TestCase subclass: CascadesTest

  testBasicCascadeInfrastructureTest =>
    // Cascade tests require compiled modules with @load (REPL cascades return futures).
    // Full cascade testing is covered in tests/e2e/cases/cascades.bt.
    // This verifies basic variable binding infrastructure used by cascades.
    x := 5.
    self assert: x equals: 5
