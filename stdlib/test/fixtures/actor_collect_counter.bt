// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// BT-904: Actor fixture for testing self-sends inside collect:/select:/reject:
// blocks. Self-sends in actor blocks need state threading because they may
// mutate actor state

Actor subclass: ActorCollectCounter
  state: count = 0

  getCount => self.count

  // Helper: increments count and returns formatted item
  processItem: item =>
    self.count := self.count + 1
    item * 2

  // collect: with self-send — safe in blocks (BT-953 lint removed as false positive).
  doubleAll: items => items collect: [:item | self processItem: item]

  // select: with self-send — same intentional pattern as doubleAll: above.
  selectPositive: items => items select: [:item | self checkPositive: item]

  // reject: with self-send — same intentional pattern as doubleAll: above.
  rejectNegative: items => items reject: [:item | self checkNegative: item]

  // collect: with field assignment in block — direct field mutation
  doubleAllWithFieldMutation: items =>
    items
      collect: [:item |
        self.count := self.count + 1
        item * 2
      ]

  // collect: as last expression (no ^) — uses last-expression handler.
  // Self-sends in blocks are safe (BT-953 lint removed).
  doubleAllImplicit: items => items collect: [:item | self processItem: item]

  // do: with self-send — already works (has state threading).
  countAll: items =>
    items do: [:item | self processItem: item]
    self.count

  // Helper for select:
  checkPositive: item =>
    self.count := self.count + 1
    item > 0

  // Helper for reject:
  checkNegative: item =>
    self.count := self.count + 1
    item < 0

  // BT-3383: collect: assigned to a local var, then used afterward (not the
  // method's last expression) — assign-RHS position, not last-expression
  // position. Returns the result's class to let the test assert species.
  doubleAllAssignThenClass: items =>
    result := items collect: [:item | self processItem: item]
    result class

  // BT-3425: same self-send collect: shape as doubleAllAssignThenClass:
  // above, but computed INSIDE an ifTrue:/ifFalse: branch (bound to a local
  // first) rather than at the method's top level — narrower variant that
  // BT-3383's fix and regression test did not cover.
  doubleAllInsideBranchAssignThenClass: items when: flag =>
    (flag
      ifTrue: [
        result := items collect: [:item | self processItem: item]
        result class
      ]
      ifFalse: [nil])

  // BT-3425 (review finding on the fix's first version): a captured LOCAL
  // other than the collect:'s own assignment target — mutated inside the
  // collect: block, not via a field write — must also be re-threaded
  // correctly out of the ifTrue: branch, not left bound to its stale
  // pre-branch value. `localCount` is bumped once per item inside the
  // block; `result` (the collect: RHS assigned to a local first) is a
  // different local from `localCount`.
  countLocalInsideBranchThenReturnLocal: items when: flag =>
    localCount := 0
    (flag
      ifTrue: [
        result := items
          collect: [:item |
            localCount := localCount + 1
            item * 2
          ]
        result
      ]
      ifFalse: [nil])
    localCount
