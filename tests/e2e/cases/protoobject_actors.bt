// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// E2E tests for ProtoObject with actor message forwarding
// Tests doesNotUnderstand and perform:withArguments:

// @load tests/e2e/fixtures/counter.bt
// @load tests/e2e/fixtures/simple_proxy.bt

// ===========================================================================
// BASIC ACTOR SPAWNING
// ===========================================================================

Counter spawn

SimpleProxy spawn

// ===========================================================================
// CLASS MESSAGE ON SPAWNED ACTORS
// ===========================================================================
// NOTE: These tests demonstrate that class message works,
// but variable persistence in REPL prevents full proxy testing for now

// Test will be expanded when variable persistence across @load is fixed
