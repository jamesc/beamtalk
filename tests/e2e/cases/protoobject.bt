// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// E2E tests for ProtoObject - REPL-specific tests
//
// Pure value tests (class, ==, ~=, perform:) are in tests/stdlib/:
//   - protoobject_manual_test.bt (class, ==, ~=, perform:)
//   - equality.bt (comprehensive equality tests)
//
// This file tests features that need REPL: actor identity, inheritance.

// ===========================================================================
// ACTOR IDENTITY TESTS
// ===========================================================================
//
// For actors (stateful objects), `==` tests whether two references
// point to the same process (pid equality)

// @load tests/e2e/fixtures/counter.bt

a := Counter spawn
// => _

b := a
// => _

c := Counter spawn
// => _

// Same reference
a == b
// => true

// Different processes
a == c
// => false

a ~= c
// => true

// ===========================================================================
// INHERITANCE TESTS
// ===========================================================================
//
// All classes (Actor, Object, etc.) should inherit from ProtoObject
// Method lookup should terminate at ProtoObject

// @load tests/e2e/fixtures/counter.bt

// Actor class has superclass
Counter superclass
// => Actor

// Root class has no superclass
ProtoObject superclass
// => nil
