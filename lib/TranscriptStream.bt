// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// TranscriptStream - Shared workspace log with pub/sub semantics
//
// TranscriptStream is a real actor (gen_server) that acts as a shared log.
// Subscribers receive output in real-time; unsubscribed output goes to buffer only.
// Analogous to Smalltalk's Transcript global.

Actor subclass: TranscriptStream
  show: value => @primitive 'show:'
  cr => @primitive 'cr'
  subscribe => @primitive 'subscribe'
  unsubscribe => @primitive 'unsubscribe'
  recent => @primitive 'recent'
  clear => @primitive 'clear'
