// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// Beamtalk - Global class for system-wide reflection
//
// Provides access to the class registry, global namespace, and
// system-wide introspection. Analogous to Smalltalk's `Smalltalk` global.

Object subclass: Beamtalk
  sealed allClasses => @primitive 'allClasses'
  sealed classNamed: className => @primitive 'classNamed:'
  sealed globals => @primitive 'globals'
