// Copyright 2026 James Casey
// SPDX-License-Identifier: Apache-2.0

// Test boundary condition: Unicode in identifiers and strings
// Validate proper UTF-8 handling in lexer and parser

// Unicode characters in identifiers (if supported)
// Note: This tests whether the lexer/parser gracefully handles or rejects Unicode
café := 42
résumé := "document"
日本語 := "Japanese"

// Unicode in string literals (should always work)
greeting := "Hello, 世界! 🌍"
emoji := "🚀 Beamtalk to the moon! 🌙"
mathematical := "π ≈ 3.14159, √2 ≈ 1.414"

// Mixed ASCII and Unicode
message := "Beamtalk supports émojis 😀 and 中文"

// Unicode in keyword message (if identifiers support it)
object café: value résumé: data

// Valid ASCII code after Unicode tests
validCode := 100 + 200
