// BT-374: Workspace binding dispatch via persistent_term.
// Transcript is a workspace binding — generates persistent_term lookup + actor send.

Actor subclass: Greeter

  greet => Transcript show: "Hello, World!"
