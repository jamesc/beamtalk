#!/usr/bin/env bash
# Copyright 2026 James Casey
# SPDX-License-Identifier: Apache-2.0
#
# Pre-push hook: run linting before pushing.
#
# - lint-rust (clippy + fmt-check-rust) always runs (~1s)
# - lint-erlang (dialyzer) runs only if Erlang sources changed (~9s)
# - lint-js runs only if JS/TS sources changed
# - lint-beamtalk (fmt-check-beamtalk) runs only if .bt/.btscript sources changed
#
# Stamp files (.lint-erlang-stamp, .lint-js-stamp, .lint-beamtalk-stamp) track the
# last-linted fingerprint of each language's sources.

set -euo pipefail

# Skip lint entirely if all changes are in non-code directories
all_changes_in_skip_dirs() {
  local changed_files merge_base
  if merge_base="$(git merge-base HEAD origin/main 2>/dev/null)"; then
    changed_files="$(git diff --name-only "$merge_base"..HEAD 2>/dev/null || true)"
  else
    changed_files="$(git diff --name-only HEAD 2>/dev/null || true)"
  fi
  if [[ -z "$changed_files" ]]; then
    return 1 # no changes detected, run lint to be safe
  fi
  while IFS= read -r file; do
    case "$file" in
      .devcontainer/*|.claude/*|.githooks/*|scripts/*|docs/*|.gitignore|.gitattributes|*.md|LICENSE*) ;;
      *) return 1 ;;
    esac
  done <<< "$changed_files"
  return 0
}

if all_changes_in_skip_dirs; then
  echo "⏭️  All changes in .devcontainer/, scripts/, or docs/ — skipping lint"
  exit 0
fi

REPO_ROOT="$(git rev-parse --show-toplevel)"
ERLANG_STAMP="$REPO_ROOT/.lint-erlang-stamp"
JS_STAMP="$REPO_ROOT/.lint-js-stamp"
BT_STAMP="$REPO_ROOT/.lint-beamtalk-stamp"

# Compute a fingerprint of tracked files matching the given patterns.
fingerprint() {
  git ls-files -- "$@" | git hash-object --stdin-paths | { sha1sum 2>/dev/null || shasum -a 1; } | cut -d' ' -f1
}

stamp_matches() {
  local stamp_file="$1"
  local current_fp="$2"
  [[ -f "$stamp_file" ]] && [[ "$(cat "$stamp_file")" == "$current_fp" ]]
}

echo "🔍 Running lint-rust..."
just lint-rust

ERLANG_FP="$(fingerprint '*.erl' '*.hrl')"
if stamp_matches "$ERLANG_STAMP" "$ERLANG_FP"; then
  echo "⏭️  lint-erlang: no Erlang changes since last lint, skipping"
else
  echo "🔬 Running lint-erlang..."
  just lint-erlang
  echo "$ERLANG_FP" > "$ERLANG_STAMP"
fi

JS_FP="$(fingerprint '*.js' '*.ts' '*.jsx' '*.tsx')"
if stamp_matches "$JS_STAMP" "$JS_FP"; then
  echo "⏭️  lint-js: no JS/TS changes since last lint, skipping"
else
  echo "🔎 Running lint-js..."
  just lint-js
  echo "$JS_FP" > "$JS_STAMP"
fi

BT_FP="$(fingerprint '*.bt' '*.btscript')"
if stamp_matches "$BT_STAMP" "$BT_FP"; then
  echo "⏭️  lint-beamtalk: no Beamtalk changes since last lint, skipping"
else
  echo "🔍 Running lint-beamtalk..."
  just lint-beamtalk
  echo "$BT_FP" > "$BT_STAMP"
fi

echo "✅ All lint checks passed"
